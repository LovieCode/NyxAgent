<template>
	<view class="page-shell rice-theme-light">
		<NyxPageHeader title="设置" subtitle="按你的聊天习惯调一调。" />
		<scroll-view class="container" :scroll-y="true">
			<view class="profile-card" @click="goSubPage('profile')">
				<view class="profile-avatar">
					<image v-if="profileAvatar != ''" :src="profileAvatar" class="profile-avatar-img" mode="aspectFill" />
					<text v-else class="profile-avatar-text">{{profileInitial}}</text>
				</view>
				<view class="profile-info">
					<text class="profile-name">{{profileDisplayName}}</text>
					<text class="profile-desc">{{profileDisplayDesc}}</text>
				</view>
				<oui-icon-unix name="right" size="15px" color="#A79DA4" />
			</view>

			<!-- 设置菜单列表 -->
			<view class="settings-menu">
				<view class="menu-item" @click="goSubPage('profile')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="user" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">用户资料</text>
							<text class="menu-desc">昵称、头像、个人简介</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('basic')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="setting" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">连接设置</text>
							<text class="menu-desc">管理提供商和 API 密钥</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('default-models')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="robot" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">默认回复方式</text>
							<text class="menu-desc">对话、识图、语音相关默认配置</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('generation')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="experiment" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">聊天偏好</text>
							<text class="menu-desc">回复长度、显示节奏和细节程度</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('plugin')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="appstore" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">插件与网络</text>
							<text class="menu-desc">下载代理和扩展配置</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('skills')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="book" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">技能库</text>
							<text class="menu-desc">技能包、多文档和联系人启用范围</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('data')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="download" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">数据与备份</text>
							<text class="menu-desc">安全备份、完整迁移、导入预览、旧数据迁移</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
				<view class="menu-divider"></view>
				<view class="menu-item" @click="goSubPage('about')">
					<view class="menu-item-left">
						<view class="menu-icon-wrap">
							<oui-icon-unix name="infomation" size="15px" color="#DB6F92" />
						</view>
						<view class="menu-text">
							<text class="menu-title">关于</text>
							<text class="menu-desc">版本信息</text>
						</view>
					</view>
					<oui-icon-unix name="right" size="14px" color="var(--rice-text-color-3)" />
				</view>
			</view>
			<view class="tabbar-list-spacer"></view>
		</scroll-view>
		<NyxTabDock current="pages/settings/settings" />
	</view>
</template>

<script lang="uts">
	/**
	 * 设置页面 - 扁平化粉白渐变设计
	 * 参考 PI Agent models.generated.ts 模型列表
	 */

	import { getAppSetting, setAppSetting } from '@/utils/database.uts'

	export default {
		data() {
			return {
				selectedProviderId: 'deepseek' as string,
				apiKey: '' as string,
				baseUrl: '' as string,
				selectedModel: 'deepseek-v4-flash' as string,
				ghProxyEnabled: false as boolean,
				ghProxyDomain: '' as string,
				showApiKey: false as boolean,
				expandBasic: true as boolean,
				expandModel: true as boolean,
				expandPlugin: true as boolean,
				expandAbout: true as boolean,
				profileNickname: '' as string,
				profileBio: '' as string,
				profileDescription: '' as string,
				profileAvatar: '' as string
			}
		},
		computed: {
			profileDisplayName() : string {
				return this.profileNickname != '' ? this.profileNickname : '未设置昵称'
			},
			profileDisplayDesc() : string {
				if (this.profileBio != '') {
					return this.profileBio
				}
				if (this.profileDescription != '') {
					return this.profileDescription
				}
				return '普通聊天、资料和偏好设置都在这里。'
			},
			profileInitial() : string {
				const name = this.profileDisplayName
				if (name != '') {
					return name.substring(0, 1)
				}
				return '眠'
			},
			pIds() : string[] {
				return [
					'anthropic', 'openai', 'deepseek', 'zai',
					'kimi-coding', 'minimax-cn', 'xiaomi', 'ant-ling', 'custom'
				]
			},
			pNames() : string[] {
				return [
					'Anthropic', 'OpenAI', 'DeepSeek', 'ZAI',
					'Kimi', 'MiniMax', 'MiMo', 'AntLing', 'Custom'
				]
			},
			pUrls() : string[] {
				return [
					'https://api.anthropic.com',
					'https://api.openai.com/v1',
					'https://api.deepseek.com',
					'https://api.z.ai/api/coding/paas/v4',
					'https://api.kimi.com/coding',
					'https://api.minimaxi.com/anthropic',
					'https://api.xiaomimimo.com/v1',
					'https://api.ant-ling.com/v1',
					''
				]
			},
			pHints() : string[] {
				return [
					'sk-ant-...', 'sk-...', 'sk-...', 'ZAI Coding Plan Key',
					'KIMI_API_KEY', 'MINIMAX_CN_API_KEY', 'XIAOMI_API_KEY', 'ANT_LING_API_KEY', 'Enter API Key'
				]
			},
			pModels() : string[][] {
				return [
					['claude-sonnet-4-20250514', 'claude-3-5-sonnet-20241022', 'claude-3-5-haiku-20241022'],
					['gpt-4o', 'gpt-4o-mini', 'gpt-4.1', 'gpt-4.1-mini', 'gpt-4.1-nano'],
					['deepseek-v4-flash', 'deepseek-v4-pro'],
					['glm-5.2', 'glm-5.1', 'glm-5-turbo', 'glm-4.7', 'glm-4.5-air'],
					['k2p7', 'kimi-for-coding', 'kimi-k2-thinking'],
					['MiniMax-M3', 'MiniMax-M2.7', 'MiniMax-M2.7-highspeed'],
					['mimo-v2.5-pro-ultraspeed', 'mimo-v2.5-pro', 'mimo-v2-pro', 'mimo-v2-omni', 'mimo-v2-flash'],
					['Ring-2.6-1T', 'Ling-2.6-1T', 'Ling-2.6-flash'],
					['custom-model']
				]
			},
			currentModels() : string[] {
				const idx = this.findProviderIndex(this.selectedProviderId)
				if (idx >= 0) {
					return this.pModels[idx]
				}
				return ['custom-model']
			},
			keyPlaceholder() : string {
				const idx = this.findProviderIndex(this.selectedProviderId)
				if (idx >= 0) {
					return this.pHints[idx]
				}
				return 'Enter API Key'
			}
		},
		onLoad() {
			this.loadSettings()
			this.loadProfileSummary()
		},
		onShow() {
			this.loadProfileSummary()
		},
		methods: {
			findProviderIndex(id : string) : number {
				for (let i = 0; i < this.pIds.length; i++) {
					if (this.pIds[i] == id) {
						return i
					}
				}
				return -1
			},

			loadSettings() {
				let providerId = getAppSetting('settings_provider_id')
				this.selectedProviderId = (providerId != null && providerId != '') ? providerId : 'deepseek'
				let key = getAppSetting('settings_api_key')
				this.apiKey = (key != null) ? key : ''
				let url = getAppSetting('settings_base_url')
				this.baseUrl = (url != null) ? url : ''
				let model = getAppSetting('settings_model')
				this.selectedModel = (model != null && model != '') ? model : 'deepseek-v4-flash'
				let proxyEnabled = getAppSetting('settings_gh_proxy_enabled')
				this.ghProxyEnabled = (proxyEnabled != null && proxyEnabled == 'true')
				let proxyDomain = getAppSetting('settings_gh_proxy_domain')
				this.ghProxyDomain = (proxyDomain != null) ? proxyDomain : ''
			},

			loadProfileSummary() {
				const nickname = getAppSetting('profile_nickname')
				this.profileNickname = (nickname != null) ? nickname : ''
				const bio = getAppSetting('profile_bio')
				this.profileBio = (bio != null) ? bio : ''
				const description = getAppSetting('profile_description')
				this.profileDescription = (description != null) ? description : ''
				const avatar = getAppSetting('profile_avatar')
				this.profileAvatar = (avatar != null) ? avatar : ''
			},

			saveSettings() {
				setAppSetting('settings_provider_id', this.selectedProviderId)
				setAppSetting('settings_api_key', this.apiKey)
				setAppSetting('settings_base_url', this.baseUrl)
				setAppSetting('settings_model', this.selectedModel)
				setAppSetting('settings_gh_proxy_enabled', this.ghProxyEnabled ? 'true' : 'false')
				setAppSetting('settings_gh_proxy_domain', this.ghProxyDomain)
				uni.showToast({ position: 'bottom', 
					title: '已保存',
					icon: 'success'
				})
			},

			selectProvider(id : string) {
				this.selectedProviderId = id
				const idx = this.findProviderIndex(id)
				if (idx >= 0) {
					this.baseUrl = ''
					const models = this.pModels[idx]
					if (models != null && models.length > 0) {
						this.selectedModel = models[0]
					}
				}
			},

			selectModel(model : string) {
				this.selectedModel = model
			},

			toggleGhProxy() {
				this.ghProxyEnabled = !this.ghProxyEnabled
			},

			toggleShowApiKey() {
				this.showApiKey = !this.showApiKey
			},

			goSubPage(name : string) {
				uni.navigateTo({
					url: '/pages/settings/' + name + '/' + name
				})
			}
		}
	}
</script>

<style>
	.page-shell {
		background-color: #FAF7F5;
	}

	.container {
		flex: 1;
		padding-left: 24px;
		padding-right: 24px;
		background-color: #FAF7F5;
	}

	.profile-card {
		flex-direction: row;
		align-items: center;
		background-color: #FFFFFF;
		border-radius: 20px;
		padding: 14px;
		margin-top: 4px;
		margin-bottom: 14px;
		border-width: 1px;
		border-style: solid;
		border-color: rgba(241, 229, 234, 0.92);
		box-shadow: 0 8px 20px rgba(216, 140, 165, 0.045);
	}

	.profile-avatar {
		width: 54px;
		height: 54px;
		border-radius: 18px;
		background-color: #F59AB7;
		align-items: center;
		justify-content: center;
		margin-right: 13px;
		overflow: hidden;
	}

	.profile-avatar-img {
		width: 54px;
		height: 54px;
		border-radius: 18px;
	}

	.profile-avatar-text {
		font-size: 21px;
		font-weight: bold;
		color: #FFFFFF;
	}

	.profile-info {
		flex: 1;
		flex-direction: column;
	}

	.profile-name {
		font-size: 16px;
		font-weight: bold;
		color: #2A2529;
		margin-bottom: 5px;
	}

	.profile-desc {
		font-size: 12px;
		color: #8A7B84;
		line-height: 18px;
	}

	/* 通用卡片容器：保留给后续设置子块复用 */
	.card {
		background-color: #FFFFFF;
		border-radius: 18px;
		padding: 16px;
		margin-bottom: 12px;
		border-width: 1px;
		border-style: solid;
		border-color: #F1E5EA;
	}

	.card-header {
		flex-direction: row;
		align-items: center;
		margin-bottom: 10px;
	}

	.card-icon {
		font-size: 14px;
		margin-right: 6px;
	}

	.card-title {
		font-size: 15px;
		font-weight: bold;
		color: #111827;
	}

	.input-row {
		flex-direction: row;
		align-items: center;
	}

	.input-row-input {
		flex: 1;
	}

	.toggle-vis-btn {
		width: 44px;
		height: 44px;
		justify-content: center;
		align-items: center;
		margin-left: 4px;
	}

	.toggle-vis-icon {
		font-size: 24px;
	}

	.input-wrap {
		margin-top: 4px;
	}

	.flat-input {
		background-color: #F9FAFB;
		border-radius: 12px;
		padding: 12px;
		font-size: 14px;
		color: #111827;
	}

	.setting-row {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		padding: 6px 0;
	}

	.setting-label {
		font-size: 14px;
		color: #374151;
	}

	.setting-value {
		font-size: 13px;
		color: #9CA3AF;
	}

	/* 设置菜单 */
	.settings-menu {
		background-color: #FFFFFF;
		border-radius: 20px;
		overflow: hidden;
		border-width: 1px;
		border-style: solid;
		border-color: rgba(241, 229, 234, 0.92);
		box-shadow: 0 8px 20px rgba(216, 140, 165, 0.045);
	}

	.menu-item {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
		padding: 13px 14px;
	}

	.menu-item-left {
		flex-direction: row;
		align-items: center;
		flex: 1;
	}

	.menu-icon-wrap {
		width: 36px;
		height: 36px;
		border-radius: 13px;
		background-color: #FFF4F7;
		align-items: center;
		justify-content: center;
		margin-right: 11px;
	}

	.menu-text {
		flex: 1;
		flex-direction: column;
	}

	.menu-title {
		font-size: 14px;
		font-weight: bold;
		color: #2A2529;
		line-height: 19px;
	}

	.menu-desc {
		font-size: 11px;
		color: #A79DA4;
		margin-top: 3px;
		line-height: 16px;
	}

	.menu-arrow {
		font-size: 16px;
		color: #D1D5DB;
		padding-left: 8px;
	}

	.menu-divider {
		height: 1px;
		background-color: #F4EAEE;
		margin-left: 61px;
	}

	.provider-grid {
		flex-direction: row;
		flex-wrap: wrap;
	}

	.provider-chip {
		background-color: #F3F4F6;
		border-radius: 10px;
		padding: 7px 14px;
		margin: 4px;
		border-width: 1px;
		border-style: solid;
		border-color: #E5E7EB;
	}

	.chip-active {
		background-color: #FDE8EF;
		border-color: #FB8FAB;
	}

	.chip-label {
		font-size: 13px;
		color: #374151;
	}

	.model-scroll {
		flex-direction: row;
		flex-wrap: wrap;
	}

	.model-pill {
		background-color: #F3F4F6;
		border-radius: 500px;
		padding: 7px 11px;
		margin: 3px;
	}

	.pill-active {
		background-color: #FDE8EF;
	}

	.pill-text {
		font-size: 12px;
		color: #6B7280;
	}

	.pill-text-active {
		color: #DB2777;
		font-weight: bold;
	}

	.save-wrap {
		padding: 10px 0 30px 0;
	}

	.rice-cell-group + .rice-cell-group {
		margin-top: 12px;
	}

	.tabbar-list-spacer {
		height: 126px;
		flex-shrink: 0;
	}
</style>
