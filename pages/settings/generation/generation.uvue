<template>
	<view class="page-shell rice-theme-light">
		<NavBar title="生成参数" showBack />
		<scroll-view class="container" :scroll-y="true">
			<view class="hero-card">
				<text class="hero-title">全局默认生成参数</text>
				<text class="hero-desc">这里是普通对话和记忆整理的默认生成参数。记忆整理参数独立保存，不会被某个智能体的人格配置带偏。</text>
			</view>

			<view class="card">
				<view class="card-header">
					<oui-icon-unix name="experiment" size="28rpx" color="#6B7280" style="margin-right:12rpx" />
					<text class="card-title">普通 Agent 参数</text>
				</view>
				<text class="field-label">温度 temperature</text>
				<input
					class="flat-input"
					type="text"
					v-model="temperatureStr"
					placeholder="0 - 2，默认 1"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">越低越稳，越高越发散。默认 1 更通用，写代码可手动调低。</text>

				<text class="field-label" style="margin-top:24rpx;">输出上限 max_tokens</text>
				<input
					class="flat-input"
					type="text"
					v-model="maxTokensStr"
					placeholder="256 - 65536，默认 4096"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">限制单次回复长度，太低会被截断，太高会更吃额度。</text>

				<text class="field-label" style="margin-top:24rpx;">Agent 最大迭代次数</text>
				<input
					class="flat-input"
					type="text"
					v-model="maxIterationsStr"
					placeholder="0 - 100，默认 20"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">Agent 每次「思考 → 工具 → 继续」算一轮。填 0 表示不限制，遇到“达到最大迭代次数”就调这里。</text>
			</view>

			<view class="card">
				<view class="card-header">
					<oui-icon-unix name="robot" size="28rpx" color="#6B7280" style="margin-right:12rpx" />
					<text class="card-title">记忆整理 Agent</text>
				</view>
				<text class="field-label">温度 temperature</text>
				<input
					class="flat-input"
					type="text"
					v-model="memoryTemperatureStr"
					placeholder="0 - 2，默认 0.7"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">默认保持通用温度，避免部分模型温度过低时异常；需要更稳再手动调低。</text>

				<text class="field-label" style="margin-top:24rpx;">输出上限 max_tokens</text>
				<input
					class="flat-input"
					type="text"
					v-model="memoryMaxTokensStr"
					placeholder="256 - 2048，默认 2048"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">记忆整理只需要简短总结，最高限制为 2048。</text>

				<text class="field-label" style="margin-top:24rpx;">最大迭代次数</text>
				<input
					class="flat-input"
					type="text"
					v-model="memoryMaxIterationsStr"
					placeholder="1 - 100，默认 20"
					placeholder-style="color: #D1D5DB; font-size: 26rpx;"
				/>
				<text class="field-tip">记忆整理会查历史、查记忆、写短缓存/长期记忆。默认 20，系统提示也会同步标注。</text>
				<text class="field-tip">记忆整理固定非流式运行，避免后台维护过程刷屏。</text>
			</view>

			<view class="card">
				<view class="setting-row">
					<view class="setting-text">
						<text class="setting-title">普通 Agent 流式显示</text>
						<text class="setting-desc">开启后边生成边显示；记忆整理 Agent 固定非流式，不受这里影响。</text>
					</view>
					<rice-switch v-model="streamEnabled" />
				</view>
			</view>

			<view class="save-wrap">
				<view class="save-btn" @click="saveSettings">
					<text class="save-btn-text">保存设置</text>
				</view>
			</view>
		</scroll-view>
	</view>
</template>

<script lang="uts">
	import { getGlobalGenerationSettings, saveGlobalGenerationSettings, getMemoryOrganizerGenerationSettings, saveMemoryOrganizerGenerationSettings, type GenerationSettings } from '@/utils/generation-settings.uts'

	export default {
		data() {
			return {
				temperatureStr: '' as string,
				maxTokensStr: '' as string,
				maxIterationsStr: '' as string,
				streamEnabled: true as boolean,
				memoryTemperatureStr: '' as string,
				memoryMaxTokensStr: '' as string,
				memoryMaxIterationsStr: '' as string,
			}
		},
		onLoad() {
			this.loadSettings()
		},
		methods: {
			clampNumber(value: number, min: number, max: number): number {
				if (value < min) return min
				if (value > max) return max
				return value
			},

			parseFloatSetting(value: string, fallback: number, min: number, max: number): number {
				const parsed = parseFloat(value)
				if (isNaN(parsed)) {
					return fallback
				}
				return this.clampNumber(parsed, min, max)
			},

			parseIntSetting(value: string, fallback: number, min: number, max: number): number {
				const parsed = parseInt(value)
				if (isNaN(parsed)) {
					return fallback
				}
				return this.clampNumber(parsed, min, max)
			},

			loadSettings() {
				const settings = getGlobalGenerationSettings()
				this.temperatureStr = settings.temperature + ''
				this.maxTokensStr = settings.maxTokens + ''
				this.maxIterationsStr = settings.maxIterations + ''
				this.streamEnabled = settings.streamEnabled
				const memorySettings = getMemoryOrganizerGenerationSettings()
				this.memoryTemperatureStr = memorySettings.temperature + ''
				this.memoryMaxTokensStr = memorySettings.maxTokens + ''
				this.memoryMaxIterationsStr = memorySettings.maxIterations + ''
			},

			saveSettings() {
				const oldSettings = getGlobalGenerationSettings()
				const oldMemorySettings = getMemoryOrganizerGenerationSettings()
				const settings: GenerationSettings = {
					temperature: this.parseFloatSetting(this.temperatureStr, oldSettings.temperature, 0, 2),
					maxTokens: this.parseIntSetting(this.maxTokensStr, oldSettings.maxTokens, 256, 65536),
					maxIterations: this.parseIntSetting(this.maxIterationsStr, oldSettings.maxIterations, 0, 100),
					streamEnabled: this.streamEnabled,
				}
				const memorySettings: GenerationSettings = {
					temperature: this.parseFloatSetting(this.memoryTemperatureStr, oldMemorySettings.temperature, 0, 2),
					maxTokens: this.parseIntSetting(this.memoryMaxTokensStr, oldMemorySettings.maxTokens, 256, 2048),
					maxIterations: this.parseIntSetting(this.memoryMaxIterationsStr, oldMemorySettings.maxIterations, 1, 100),
					streamEnabled: false,
				}
				saveGlobalGenerationSettings(settings)
				saveMemoryOrganizerGenerationSettings(memorySettings)
				this.temperatureStr = settings.temperature + ''
				this.maxTokensStr = settings.maxTokens + ''
				this.maxIterationsStr = settings.maxIterations + ''
				this.memoryTemperatureStr = memorySettings.temperature + ''
				this.memoryMaxTokensStr = memorySettings.maxTokens + ''
				this.memoryMaxIterationsStr = memorySettings.maxIterations + ''
				uni.showToast({ position: 'bottom', 
					title: '已保存',
					icon: 'success'
				})
			},
		}
	}
</script>

<style>
	.container {
		flex: 1;
		padding: 32rpx;
	}

	.hero-card {
		background-color: #FFFFFF;
		border-radius: 20rpx;
		padding: 32rpx;
		margin-bottom: 24rpx;
	}

	.hero-title {
		font-size: 32rpx;
		font-weight: bold;
		color: #111827;
		margin-bottom: 12rpx;
	}

	.hero-desc {
		font-size: 25rpx;
		color: #6B7280;
		line-height: 38rpx;
	}

	.card {
		background-color: #FFFFFF;
		border-radius: 20rpx;
		padding: 32rpx;
		margin-bottom: 24rpx;
	}

	.card-header {
		flex-direction: row;
		align-items: center;
		margin-bottom: 22rpx;
	}

	.card-title {
		font-size: 30rpx;
		font-weight: bold;
		color: #111827;
	}

	.field-label {
		font-size: 26rpx;
		color: #374151;
		font-weight: bold;
		margin-bottom: 12rpx;
	}

	.field-tip {
		font-size: 23rpx;
		color: #9CA3AF;
		line-height: 34rpx;
		margin-top: 10rpx;
	}

	.flat-input {
		background-color: #F9FAFB;
		border-radius: 14rpx;
		padding: 24rpx;
		font-size: 28rpx;
		color: #111827;
	}

	.setting-row {
		flex-direction: row;
		justify-content: space-between;
		align-items: center;
	}

	.setting-text {
		flex: 1;
		padding-right: 24rpx;
	}

	.setting-title {
		font-size: 28rpx;
		font-weight: bold;
		color: #111827;
		margin-bottom: 8rpx;
	}

	.setting-desc {
		font-size: 24rpx;
		color: #6B7280;
		line-height: 36rpx;
	}

	.save-wrap {
		padding: 20rpx 0 60rpx 0;
	}

	.save-btn {
		background-color: var(--rice-primary-color);
		border-radius: 16rpx;
		padding: 28rpx 0;
		align-items: center;
		justify-content: center;
	}

	.save-btn-text {
		font-size: 30rpx;
		font-weight: bold;
		color: #FFFFFF;
	}
</style>
